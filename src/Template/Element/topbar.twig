<cl-topbar>
    {% for child in menu.children %}
        <a href="{{ Url.build({
            _name: 'pages:objects',
            _entity: child,
        }) }}">{{ child.title|raw }}</a>
    {% endfor %}
</cl-topbar>
