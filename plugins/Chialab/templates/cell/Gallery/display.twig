{{ element('gallery', {
    object,
    defaultLayout: defaultLayout|default('grid'),
    showTitle: showTitle is defined ? showTitle : true,
    showDescription: showDescription is defined ? showDescription : false,
}) }}
