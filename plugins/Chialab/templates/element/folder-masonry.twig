<dna-masonry class="w-full grid m-0">
    {% for item in children %}
        {{ cell('Chialab.Card::collapsable', { object: item }, {
            cache: {
                key: 'card_collapsable_' ~ locale ~ '_' ~ item.id,
            },
        }) }}
    {% endfor %}
</dna-masonry>
